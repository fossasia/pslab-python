language: python

sudo: true

python:
  - "3.5"

before_install:
    - sudo apt-get -qq update
    - sudo mkdir -p /builds
    - sudo chmod a+rw /builds

install:
    - pip install -r requirements.txt
    - make clean
    - make all
    - sudo make install

script: nosetests